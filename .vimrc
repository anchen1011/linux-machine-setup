" enable mouse
set mouse=a

" enable autoindent, using space to indent
set autoindent
set expandtab

" use the general clipboards, paste from a windows or from vim, visual selection automatically copied to the clipboard
set clipboard+=unnamed
set clipboard+=unnamedplus
set paste
set go+=a

" enable cross-line motion
set whichwrap+=<,>,h,l
